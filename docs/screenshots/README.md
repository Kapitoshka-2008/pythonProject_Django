Screenshots for assignment 5

Please add the following Django shell screenshots:

1. Running `python manage.py shell`.
2. Importing a model (e.g. `from catalog.models import Category, Product`).
3. Creating and saving an instance.
4. Querying objects (e.g. `Category.objects.count()` and `Product.objects.count()`).
5. Filtering/ordering query examples.

Name files meaningfully, for example:

- `01_shell_start.png`
- `02_import_models.png`
- `03_create_objects.png`
- `04_query_counts.png`
- `05_filter_order.png`


